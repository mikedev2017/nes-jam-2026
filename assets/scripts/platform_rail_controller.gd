extends Node2D

@export var is_top_left_corner: bool = false
@export var is_bottom_left_corner: bool = false
@export var is_top_right_corner: bool = false
@export var is_bottom_right_corner: bool = false

# Called when the node enters the scene tree for the first time.
func _ready() -> void:
	pass # Replace with function body.


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta: float) -> void:
	pass
